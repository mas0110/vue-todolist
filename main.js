const { createApp  } = Vue 

    createApp({
        data() {
            return {
                // array di oggetti con contenuti del carosello

            }
        },
        methods:{

        },
    }).mount('#app')